name: Update LiaScript README from course page

on:
  workflow_dispatch:
    inputs:
      course_url:
        description: "Course page URL. Leave empty to detect a URL from README.md."
        required: false
        default: ""
  schedule:
    - cron: "0 6 * * 1"
  push:
    paths:
      - "README.md"
      - "scripts/update_readme_from_jsonld.py"
      - ".github/workflows/update-readme-from-jsonld.yml"

permissions:
  contents: write
  pull-requests: write

jobs:
  update-readme:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Update LiaScript README from course page
        env:
          COURSE_URL: ${{ github.event.inputs.course_url }}
        run: |
          set -euo pipefail
          if [ -n "${COURSE_URL:-}" ]; then
            python scripts/update_readme_from_jsonld.py \
              --url "$COURSE_URL" \
              --readme README.md \
              --debug-html fetched-course-page.html
          else
            python scripts/update_readme_from_jsonld.py \
              --readme README.md \
              --debug-html fetched-course-page.html
          fi

      - name: Upload fetched HTML for debugging
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: fetched-course-page
          path: fetched-course-page.html
          if-no-files-found: ignore

      - name: Show README changes
        run: git diff -- README.md

      - name: Create pull request if README changed
        uses: peter-evans/create-pull-request@v6
        with:
          commit-message: "Update LiaScript README metadata from course page"
          title: "Update LiaScript README metadata from course page"
          body: |
            This automated PR updates README.md using the existing LiaScript markdown dialect.
            The script first attempts to extract JSON-LD from the course page and falls back
            to parsing visible HTML sections if JSON-LD is not present in the fetched page.
          branch: "automation/update-liasiscript-readme"
          delete-branch: true
